// Note: The main / route is the dashboard.
// To view the site's homepage (/), we expose it at /site/home or via a separate preview route.
// The public slug renderer at /[slug] handles all non-root paths.
// This file is intentionally empty - the homepage is handled by src/app/page.jsx (dashboard).
// To preview the home page, visit /preview in the browser.
export {};
