import { pageElementMap } from '../elements/page/pageElementMap';

export const pageCanvasConfig = {
  singleSection: false,
  allowMultipleSections: true,
  elementMap: pageElementMap,
  defaultState: { sections: [] },
};
